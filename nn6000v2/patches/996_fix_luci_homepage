#!/bin/sh
######################################################################
# vim: set ft=sh
# DEPRECATED: 此 uci-defaults 已废弃，逻辑移至 fix_luci_homepage init.d 脚本。
#
# 原因: uwsgi (S79) 构建路由缓存时 quickstart (S92) 尚未启动，
#       仅在首次启动清缓存不够，每次启动都需要清。
#       uci-defaults 只执行一次就删除，无法覆盖后续启动。
#       现由 fix_luci_homepage (init.d, START=93) 在每次启动时处理。
#
# 保留此文件仅为兼容旧 overlay（防止旧备份还原后残留的 996 覆盖
# 新固件中的 fix_luci_homepage init.d 脚本）。
######################################################################
exit 0
